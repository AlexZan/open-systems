The thought is that every single action that can be taken is an event
that executes middleware. This is similar to how services are written in
node express. This would be a democratized middleware.

Example:

When a report is made, middleware that has been accepted by the
community or globally can be executed to determine if the report is
urgent or normal.

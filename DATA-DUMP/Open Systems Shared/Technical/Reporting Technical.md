The default reporting action will be to execute matching report types.

if(type == "badtag")

Execute consensus group with specific report rules.

Consensus group

-send out invitation to be jury to exp top half of the users at random,
if possible exclude reporte and reporter. Wait double the time it took
to get double to accepts of the maximum required to participate. E.g. if
2 jury needed, and it took 10 minutes to get 2 \* 2 users accepting,
then wait 20 minutes to get more accepts, then select 2 at random from
those.

-select max two random auditors, using the same logic, but for the
specific tag.

Will need to know the reportee and reporter, this will need to be passed
in

## Technical Use Case

1.  Client submits report with

    a.  Post id

    b.  Report type

    c.  Signature on sign offs

        i.  You understand that reports must be urgent and you swear
            this report is urgent.
